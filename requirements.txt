# core pipeline
langchain-ollama>=0.2.0
langchain-core>=0.3.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
sentence-transformers>=2.2
transformers>=4.35
torch>=2.0

# LoRA fine-tuning, needed only for step10_exp5_oneshot.py
peft>=0.7.0
accelerate>=0.25.0

# layer-wise probing, needed only for run_jlens_probe.py
# not on PyPI, install from source:
#   pip install git+https://github.com/anthropics/jacobian-lens
# prepare_fit_prompts.py will pull WikiText-103 through `datasets` if it is
# available, and fall back to a built-in passage set if not
datasets>=2.14
